import Foundation
import KeyboardShortcuts

final class HotkeyManager {
    // Hotkey registration is handled via KeyboardShortcuts package
    // in AppState.setupHotkey(). This class exists for future
    // expansion (e.g., multiple hotkeys).
}
